"""
Pure-Python algorithm ports from pyqtgraph (MIT-licensed).

Each module in this package is a stand-alone port of one or more
algorithms from upstream pyqtgraph, refactored to drop pyqtgraph's
``QGraphicsScene`` plumbing and made consumable from the existing
``QQuickPaintedItem``-based TRANS canvases. The original ``LICENSE`` is
preserved next to this ``__init__.py``; every ported file carries a
header noting the upstream module + commit hash it was derived from.

Upstream: https://github.com/pyqtgraph/pyqtgraph
"""
